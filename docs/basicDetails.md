Basic commands:

1. npm run migration:generate src/database/migrations/YourMigrationName (Generate migration)
2. npm run migration:create src/database/migrations/YourMigrationName (Create empty migration)
3. npm run migration:run (run migration)
4. npm run migration:revert (revert migration)
